# Scholarship Radar public mirror

This repository publishes a read-only mirror of the public Scholarship Radar catalog for networks that block Cloudflare's `workers.dev` hostnames.

The scheduled workflow copies only public pages and public assets. It does not contain credentials, crawler data, database exports, admin pages, account pages, or submission APIs.
